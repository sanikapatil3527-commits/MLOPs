# Day 3 — CI/CD + Cloud + Model Registry (Local)

This repo is the continuation of Day 2:
**Data → Validation → Training → Evaluation → Registry → Deploy**

## 1) Setup
```bash
python -m venv .venv
# Windows
.\.venv\Scripts\activate
# macOS/Linux
source .venv/bin/activate

pip install -r requirements.txt
